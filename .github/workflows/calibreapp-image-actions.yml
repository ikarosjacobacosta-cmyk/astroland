name: Compress and Convert Images to WebP

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'
  pull_request:
    paths:
      - '**.jpg'
      - '**.jpeg'
      - '**.png'
      - '**.webp'

jobs:
  build:
    name: Optimize Images
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Compress and Convert Images
        id: calibre
        uses: calibreapp/image-actions@main
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # ← Cambiado a mayúsculas
          webpQuality: '80'                           # ← Eliminado "export-webp"
          compressOnly: ${{ github.event_name != 'pull_request' }}

      - name: Create Pull Request
        if: |
          github.event_name != 'pull_request' &&
          steps.calibre.outputs.markdown != ''
        uses: peter-evans/create-pull-request@v6
        with:
          title: 'Auto: Compress and Convert Images to WebP'
          branch-suffix: timestamp
          commit-message: 'Compress and convert images to WebP'
          body: ${{ steps.calibre.outputs.markdown }}
